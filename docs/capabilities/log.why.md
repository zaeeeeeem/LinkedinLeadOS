# `log.why` — contract

The contract lives next to the capability, per spec §10 (one directory an agent can read
cold): [`src/capabilities/log.why/README.md`](../../src/capabilities/log.why/README.md).
