# `log.runs` — contract

The contract lives next to the capability, per spec §10 (one directory an agent can read
cold): [`src/capabilities/log.runs/README.md`](../../src/capabilities/log.runs/README.md).
