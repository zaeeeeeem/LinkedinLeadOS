# `log.errors` — contract

The contract lives next to the capability, per spec §10 (one directory an agent can read
cold): [`src/capabilities/log.errors/README.md`](../../src/capabilities/log.errors/README.md).
